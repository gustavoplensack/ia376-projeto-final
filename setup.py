from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Pre-training of an OCR-like model in Wikipedia dataset.',
    author='gustavoplensack',
    license='MIT',
)
